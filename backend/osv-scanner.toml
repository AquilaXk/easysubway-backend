# Existing vulnerable backend Gradle lockfile advisories surfaced when the
# dependency gate was first enabled. Keep these exceptions vulnerability-id
# scoped and adjacent to this lockfile so new advisories still fail the PR gate.

[[IgnoredVulns]]
id = "GHSA-3pxv-7cmr-fjr4"
reason = "기존 backend Gradle lockfile 기준선에서 발견된 취약점은 별도 업그레이드 작업으로 처리한다."

[[IgnoredVulns]]
id = "GHSA-445c-vh5m-36rj"
reason = "기존 backend Gradle lockfile 기준선에서 발견된 취약점은 별도 업그레이드 작업으로 처리한다."

[[IgnoredVulns]]
id = "GHSA-6hg6-v5c8-fphq"
reason = "기존 backend Gradle lockfile 기준선에서 발견된 취약점은 별도 업그레이드 작업으로 처리한다."
