# backend Gradle lockfile 취약점 예외 목록. 예외는 취약점 ID 단위로 좁게, 사유를
# lockfile 옆에 유지한다 — 새 advisory는 PR 게이트에서 실패한다.
# 새 ignore는 ignoreUntil + reason + tracking 이슈가 필요하다 / New ignores need ignoreUntil + reason + tracking.
